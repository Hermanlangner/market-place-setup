---
name: shared-kit-roll
description: Roll two six-sided dice. Use when the user says "roll the dice".
---

# Roll

Run the shared dice script with Bash:

```
"${CLAUDE_PLUGIN_ROOT}/scripts/roll.sh"
```

Show its output verbatim. Add nothing else.
