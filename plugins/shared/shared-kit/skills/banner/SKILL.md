---
description: Print a boxed ASCII banner. Use when the user asks for a banner, e.g. "banner HELLO".
---

# Banner

Run the shared banner script with Bash, passing the user's text as the only argument:

```
"${CLAUDE_PLUGIN_ROOT}/scripts/banner.sh" "<text>"
```

Show its output verbatim. Add nothing else.
