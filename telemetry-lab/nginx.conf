server {
  listen 80;

  # Proxy the collector's Prometheus endpoint so the dashboard can fetch
  # same-origin (no CORS setup needed).
  location /metrics {
    proxy_pass http://otel-collector:8889/metrics;
  }

  location / {
    root /usr/share/nginx/html;
    try_files $uri /index.html;
  }
}
